# ✅ 腾讯云服务器连接成功报告

**连接时间**: 2025-11-18 14:55:14 CST  
**状态**: ✅ **连接成功**

---

## 📊 服务器信息

### 基本信息
- **公网IP**: 49.51.244.154
- **内网IP**: 172.26.16.12
- **主机名**: VM-16-12-ubuntu
- **用户名**: ubuntu

### 系统配置
- **操作系统**: Ubuntu 22.04 LTS (Jammy Jellyfish)
- **内核版本**: Linux 5.15.0-153-generic
- **架构**: x86_64 (64位)

### 硬件资源
- **CPU**: x86_64
- **内存**: 30 GB
  - 已使用: 336 MB (1%)
  - 可用: 30 GB
- **磁盘**: 99 GB
  - 已使用: 4.2 GB (5%)
  - 可用: 91 GB
- **Swap**: 0 GB (未配置)

### 系统状态
- **系统负载**: 0.046875 (非常低)
- **运行进程**: 164
- **登录用户**: 1
- **运行时间**: 正常运行中

---

## 🔑 连接方式

### SSH连接命令
```bash
ssh -i mishi/lianxi.pem ubuntu@49.51.244.154
```

### 文件位置
- **密钥文件**: `mishi/lianxi.pem`
- **IP地址文件**: `ip.txt`

### 连接参数
- **协议**: SSH
- **端口**: 22 (默认)
- **认证方式**: RSA私钥认证
- **用户**: ubuntu
- **权限**: sudo权限可用

---

## 🛠️ 常用操作

### 1. 基本命令测试
```bash
# 查看当前用户
whoami
# 输出: ubuntu

# 查看系统信息
uname -a
# 输出: Linux VM-16-12-ubuntu 5.15.0-153-generic

# 查看磁盘使用
df -h
# 根分区: 99G, 已用4.2G, 可用91G

# 查看内存使用
free -h
# 总内存: 30GB, 已用336MB, 可用30GB
```

### 2. 使用sudo权限
```bash
# 以管理员身份运行命令
sudo <command>

# 例如：更新软件包
sudo apt update
sudo apt upgrade
```

### 3. 文件传输

#### 从本地上传到服务器
```bash
# 上传单个文件
scp -i mishi/lianxi.pem <本地文件> ubuntu@49.51.244.154:~/

# 上传整个目录
scp -i mishi/lianxi.pem -r <本地目录> ubuntu@49.51.244.154:~/
```

#### 从服务器下载到本地
```bash
# 下载单个文件
scp -i mishi/lianxi.pem ubuntu@49.51.244.154:~/<远程文件> <本地路径>

# 下载整个目录
scp -i mishi/lianxi.pem -r ubuntu@49.51.244.154:~/<远程目录> <本地路径>
```

### 4. 断开连接
```bash
# 在SSH会话中输入
exit
# 或按 Ctrl+D
```

---

## 📦 推荐配置

### 1. 安装常用软件
```bash
# 更新软件包列表
sudo apt update

# 安装基础工具
sudo apt install -y git vim curl wget htop

# 安装Python环境
sudo apt install -y python3 python3-pip

# 安装Docker (可选)
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
```

### 2. 配置防火墙
```bash
# 查看防火墙状态
sudo ufw status

# 允许SSH
sudo ufw allow 22/tcp

# 启用防火墙
sudo ufw enable
```

### 3. 创建Swap (推荐)
```bash
# 创建4GB swap文件
sudo fallocate -l 4G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile

# 永久启用
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
```

---

## 🔒 安全建议

### 1. 密钥文件安全
- ✅ 密钥文件已保存在 `mishi/lianxi.pem`
- ⚠️ **不要**将密钥文件上传到GitHub
- ✅ 已在 `.gitignore` 中排除 `mishi/` 目录

### 2. SSH安全配置
```bash
# 禁用密码登录（只允许密钥登录）
sudo vim /etc/ssh/sshd_config
# 设置: PasswordAuthentication no

# 重启SSH服务
sudo systemctl restart sshd
```

### 3. 定期更新系统
```bash
# 每周运行一次
sudo apt update && sudo apt upgrade -y
```

---

## 📝 快速连接脚本

### Windows PowerShell
创建文件 `connect_tencent.ps1`:
```powershell
# 连接到腾讯云服务器
ssh -i mishi/lianxi.pem ubuntu@49.51.244.154
```

使用方法:
```powershell
.\connect_tencent.ps1
```

### Linux/Mac Bash
创建文件 `connect_tencent.sh`:
```bash
#!/bin/bash
# 连接到腾讯云服务器
ssh -i mishi/lianxi.pem ubuntu@49.51.244.154
```

使用方法:
```bash
chmod +x connect_tencent.sh
./connect_tencent.sh
```

---

## 🎯 下一步建议

### 1. 部署OFI项目到服务器
```bash
# 在服务器上克隆项目
git clone https://github.com/chensirou3/Order-Flow-Imbalance-analysis.git
cd Order-Flow-Imbalance-analysis

# 安装依赖
pip3 install -r requirements.txt

# 上传数据文件
# 从本地上传tick数据到服务器
```

### 2. 配置开发环境
- 安装Jupyter Notebook
- 配置Python虚拟环境
- 安装数据分析工具

### 3. 设置定时任务
- 使用cron定时运行分析脚本
- 自动备份结果文件

---

## ✅ 连接验证清单

- [x] SSH连接成功
- [x] 密钥认证正常
- [x] 用户权限确认 (ubuntu with sudo)
- [x] 系统信息获取
- [x] 磁盘空间充足 (91GB可用)
- [x] 内存充足 (30GB)
- [x] 网络连接正常

---

## 📞 连接信息摘要

```
主机: 49.51.244.154
用户: ubuntu
密钥: mishi/lianxi.pem
系统: Ubuntu 22.04 LTS
内存: 30GB
磁盘: 99GB (91GB可用)
状态: ✅ 运行正常
```

**连接命令**:
```bash
ssh -i mishi/lianxi.pem ubuntu@49.51.244.154
```

---

**测试时间**: 2025-11-18 14:55  
**测试结果**: ✅ 成功  
**服务器状态**: 🟢 正常运行

